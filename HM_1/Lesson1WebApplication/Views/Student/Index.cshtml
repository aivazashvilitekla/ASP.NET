@model IEnumerable<Lesson1WebApplication.Models.StudentViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FullName)
        </th>
        <th>

        </th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.FullName)
    </td>
    <td>
        @Html.ActionLink("რედაქტირება", "Edit", new { /* id=item.PrimaryKey */ })
        @Html.ActionLink("დეტალები", "Details", new { id = item.ID })
        @Html.ActionLink("წაშლა", "Delete", new { /* id=item.PrimaryKey */ })
    </td>
</tr>
}

</table>
